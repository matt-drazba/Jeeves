# Copy this to .env and fill in values — .env is gitignored and never committed
# Calendar is NOT configured here — it comes from HA's calendar API (see CLAUDE.md).
HA_TOKEN=                 # HA long-lived token (Profile → Security → Long-lived access tokens)
PURPLEAIR_API_KEY=        # PurpleAir read API key
